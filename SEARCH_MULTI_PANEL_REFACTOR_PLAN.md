# SP 多资源面板中文搜索改造方案

## 1. 背景

当前插件已经能翻译 Substance 3D Painter 主资源库，以及滤镜、生成器、材质、着色器等资源选择器中的资源名称。

但是中文搜索目前只绑定一组控件，并且只严格识别主资源库：

- 容器：`Alg::NewResourcesView`
- 搜索框：`Alg::SearchFieldLineEdit`，`objectName=search_field`
- 列表：`Alg::ResourceListView`，`objectName=resources`
- 模型：`Alg::NewResourceListModel`

滤镜和生成器等弹窗使用 `Alg::ResourcePickerWidget`，因此列表名称可以翻译，但其中的搜索框没有进入中文搜索逻辑。

此外，现有 `AssetRowFilter` 只有一组 `container_ / field_ / view_ / model_` 状态。即使放宽识别条件，也会在主资源库和临时资源选择器之间反复抢占绑定，存在查询串台、隐藏行残留和空搜索错误过滤的风险。

## 2. 改造目标

同时支持以下搜索位置：

1. Painter 主资源库。
2. 滤镜选择器。
3. 生成器选择器。
4. 材质、智能材质、着色器及其他使用 `Alg::ResourcePickerWidget` 的资源选择器。

必须保持以下行为：

- 空搜索交给 Painter 原生搜索，并显示当前类别的全部资源。
- 不含中文的搜索完全交给 Painter 原生搜索。
- 只有包含中文时才启用插件的本地过滤。
- 不替换 Painter 的模型，不影响资源激活、拖放、选择和资源 URL。
- 每个搜索面板互相独立，一个面板的查询不能影响另一个面板。
- 插件禁用、卸载或面板关闭后，不得残留隐藏行或隐藏的原生查询状态。

## 3. 总体架构

将当前单状态的 `AssetRowFilter` 改造成一个搜索管理器：

```text
AssetSearchManager
├─ SearchSurface：主资源库
├─ SearchSurface：滤镜选择器
├─ SearchSurface：生成器选择器
└─ SearchSurface：其他资源选择器
```

每个 `SearchSurface` 独立保存：

- `container`：搜索框和列表共同所属的容器。
- `field`：该面板的搜索框。
- `view`：该面板的资源列表。
- `model`：列表当前使用的模型。
- `query`：当前中文查询。
- `timer`：该面板独立的防抖计时器。
- `fieldConnection`：搜索框信号连接。
- `modelConnections`：模型重置、增删行和布局变化连接。
- `localQuery`：是否由插件接管当前中文查询。
- `applying`：防止插件改写搜索框时递归触发。

管理器负责发现、创建、更新和销毁这些 `SearchSurface`，不直接保存某一个面板的查询状态。

## 4. 面板识别规则

### 4.1 主资源库

继续使用现有严格规则，避免误绑定普通列表：

- 最近的资源容器为 `Alg::NewResourcesView`。
- 搜索框类为 `Alg::SearchFieldLineEdit`。
- 搜索框 `objectName` 为 `search_field`。
- 列表类为 `Alg::ResourceListView`。
- 列表 `objectName` 为 `resources`。
- 模型类为 `Alg::NewResourceListModel`。

### 4.2 滤镜、生成器等资源选择器

统一识别最近的 `Alg::ResourcePickerWidget`，不单独猜测“滤镜”或“生成器”。

配对要求：

1. 搜索框和资源列表必须属于同一个最近的 `Alg::ResourcePickerWidget`。
2. 列表必须满足现有 `isResourcePickerView()` 判断。
3. 搜索框优先要求类名为 `Alg::SearchFieldLineEdit`。
4. 若 Painter 版本中的 `objectName` 与主资源库不同，应以类名和共同祖先为主要依据，`objectName` 只作为辅助条件。
5. 一个容器发现多个候选搜索框或列表时，不应盲目选择第一个；优先选择可见、启用且与列表拥有最近共同父级的组合。
6. 无法唯一配对时放弃绑定，保持原生行为。

模型只需要提供稳定的 `Qt::DisplayRole` 和行隐藏能力。首次实现时记录实际模型类名，确认不同 Painter 版本后再建立白名单；未知模型应安全退出，不能强行过滤。

## 5. 中文搜索流程

每个 `SearchSurface` 独立执行以下流程。

### 5.1 输入为空

1. 停止本面板计时器。
2. 清除本面板的本地中文查询状态。
3. 取消本列表的全部 `setRowHidden()`。
4. 不再向搜索框写入任何文本。
5. 让 Painter 正常处理空查询。

这是最高优先级的“失败时全部显示”规则。

### 5.2 输入不含中文

1. 清除本面板的本地隐藏行。
2. 退出本地查询模式。
3. 不阻止、不改写 Painter 收到的搜索文本。

英文、数字和符号搜索继续完全使用 Painter 原生逻辑。

### 5.3 输入包含中文

1. 保存用户可见查询、光标和选择范围。
2. 暂时向 Painter 发送空查询，让其恢复当前资源类别的完整模型。
3. 使用 `QSignalBlocker` 恢复搜索框中用户看到的中文文本。
4. 从每一行的 `Qt::DisplayRole` 读取英文原文。
5. 查找对应中文译文。
6. 将“英文原文 + 中文译文”规范化后匹配查询词。
7. 只对本面板列表调用 `setRowHidden()`。

不使用中文反查英文，也不替换 Painter 的原生模型。

## 6. 生命周期处理

### 控件出现

应用级事件过滤器发现 `Alg::NewResourcesView` 或 `Alg::ResourcePickerWidget` 的子控件时，通知管理器重新检查对应容器。

### 模型被替换

Painter 可能保留搜索框和列表，只替换模型。此时：

- 若新模型仍受支持，更新当前 `SearchSurface` 的模型连接并重新过滤。
- 不把可见中文查询瞬间发送给 Painter 原生搜索。
- 若新模型不受支持，恢复全部行并解除该面板绑定。

### 面板关闭或控件销毁

所有对象使用 `QPointer`。定期清理失效的 `SearchSurface`，断开连接并停止其计时器。

### 插件禁用或卸载

遍历所有 `SearchSurface`：

1. 停止计时器。
2. 清除隐藏行。
3. 如果搜索框显示的中文此前通过阻塞信号恢复，则向 Painter 补发一次原生 `textChanged`。
4. 断开所有连接。
5. 清空管理器。

## 7. 必须避免的问题

- 不允许使用单个全局计时器处理所有面板。
- 不允许新打开的资源选择器覆盖主资源库绑定。
- 不允许按全局查找结果随意配对搜索框和列表。
- 不允许空查询继续使用旧中文查询过滤。
- 不允许计时器对已经销毁或已经更换模型的列表执行过滤。
- 不允许非中文查询经过插件过滤。
- 不允许一个面板清除查询时解除另一个面板的隐藏行。
- 不允许为了搜索而替换 `QAbstractItemModel` 或安装代理模型。

## 8. 测试清单

### 主资源库

- 中文搜索正常。
- 英文搜索仍为原生行为。
- 清空搜索立即恢复全部资源。
- 快速输入、删除和切换分类不出现空白列表。

### 滤镜和生成器选择器

- 中文名称可以搜索。
- 英文名称仍可原生搜索。
- 清空后恢复该类别全部资源。
- 关闭再打开面板不保留上次隐藏行。
- 在两个不同选择器之间切换不串查询。

### 并存状态

- 主资源库保留中文查询时打开生成器选择器，二者互不影响。
- 生成器选择器中文搜索时关闭弹窗，主资源库状态不改变。
- 多个资源选择器短时间连续创建和销毁时不崩溃。

### 开关和卸载

- 关闭“中文搜索”或翻译总开关后全部行恢复。
- 重新启用后只接管仍然包含中文的搜索框。
- 重新载入插件后不存在隐藏行残留。

### 自动回归检查

- 保留“空查询失败时全部显示”的静态回归断言。
- 增加管理多个 `SearchSurface` 的结构断言。
- 增加 `Alg::ResourcePickerWidget` 搜索容器识别断言。
- 增加每个面板独立计时器和独立连接的断言。
- 增加非中文查询退出本地模式的断言。

## 9. 实施顺序

1. 将当前 `AssetRowFilter` 的单面板字段提取为 `SearchSurface`。
2. 保持主资源库逻辑不变，先用新架构通过原有测试。
3. 增加 `Alg::ResourcePickerWidget` 容器发现和控件配对。
4. 增加多面板生命周期清理。
5. 增加自动回归测试。
6. 编译 Qt5、Qt6 原生模块并重新打包。
7. 先在 Painter 主资源库回归，再测试滤镜和生成器面板。

## 10. 当前状态

多面板管理器已经实现：每个主资源库或 `Alg::ResourcePickerWidget` 都拥有独立的 `AssetRowFilter`、计时器、查询和模型连接。主资源库仍使用原有严格识别规则，资源选择器按共同祖先配对搜索框与列表；容器销毁时采用静默清理，正常禁用或卸载时恢复 Painter 原生查询。

Qt5、Qt6 原生模块已经通过编译，自动回归测试已经通过。下一步需要安装到 Painter，实测滤镜、生成器等资源选择器的实际搜索框和模型类名；若某个 Painter 版本使用不同的列表类名，再根据嗅探结果补充严格白名单。
