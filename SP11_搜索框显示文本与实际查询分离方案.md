# SP11 搜索框显示文本与实际查询分离方案

## 定位

本方案只解决一个问题：让 Painter 内部收到的“实际查询文本”和搜索框最终显示的“可见文本”不同。

它不负责生成实际查询文本，不包含中文转英文逻辑，也不需要维护中英文映射词典。实际查询文本必须由调用方通过其他可靠方式提供。

SP11 调试实测对象：

- 搜索框：`Alg::SearchFieldLineEdit`，`objectName=search_field`
- 资产视图：`Alg::ResourceListView`，`objectName=resources`
- 资产模型：`Alg::NewResourceListModel`
- Qt：6.5.8

SP11 的资产模型直接挂在视图上，不是 `QSortFilterProxyModel`，因此本方案不修改 `DisplayRole`、不设置 `filterRole`、不替换资产模型。

## 工作流程

假设：

- `actual_query`：需要交给 Painter 原生搜索的文本
- `visible_text`：需要留在搜索框里给用户看的文本

执行顺序必须保持如下：

1. 保存光标位置、选区和搜索框原来的信号阻断状态。
2. 保持信号开启，调用 `setText(actual_query)`。
3. Painter 原生 `textChanged` 接收并保存实际查询。
4. 暂时阻断搜索框发出的全部信号。
5. 调用 `setText(visible_text)`，只恢复界面显示。
6. 恢复搜索框原来的信号阻断状态。
7. 恢复光标和选区。

最终状态：

```text
Painter 内部查询状态 = actual_query
搜索框可见内容       = visible_text
```

SP11 运行测试证明：恢复可见文本两秒后，Painter 的搜索结果仍按先前收到的实际查询生成，说明当前版本不会在稍后重新读取 `QLineEdit.text()` 覆盖内部查询。

## PySide6 参考实现

```python
from PySide6 import QtCore


class SearchTextPresenter(QtCore.QObject):
    """把查询文本发送给 Painter，再恢复搜索框可见文本。"""

    def __init__(self, search_field):
        super().__init__(search_field)
        self._field = search_field
        self._applying = False

    def apply(self, actual_query: str, visible_text: str) -> bool:
        field = self._field

        if self._applying or field is None:
            return False
        if actual_query is None or visible_text is None:
            return False

        actual_query = str(actual_query)
        visible_text = str(visible_text)

        cursor = field.cursorPosition()
        selection_start = field.selectionStart()
        selection_length = len(field.selectedText())

        self._applying = True
        try:
            # 这一遍不能阻断信号：Painter 必须收到实际查询。
            field.setText(actual_query)

            # 只恢复显示，不让 Painter 收到 visible_text。
            previous_block_state = field.blockSignals(True)
            try:
                field.setText(visible_text)

                if selection_start >= 0:
                    start = min(selection_start, len(visible_text))
                    length = min(
                        selection_length,
                        len(visible_text) - start,
                    )
                    field.setSelection(start, max(0, length))
                else:
                    field.setCursorPosition(
                        min(cursor, len(visible_text))
                    )
            finally:
                field.blockSignals(previous_block_state)
        finally:
            self._applying = False

        return True
```

调用示例：

```python
presenter.apply(
    actual_query="Painter 应当接收的查询",
    visible_text="搜索框最终显示的文字",
)
```

## C++/Qt 参考实现

```cpp
bool applyQueryWithVisibleText(QLineEdit *field,
                               const QString &actualQuery,
                               const QString &visibleText) {
    if (!field)
        return false;

    const int cursor = field->cursorPosition();
    const int selectionStart = field->selectionStart();
    const int selectionLength = field->selectedText().size();

    // 信号保持开启：Painter 原生搜索接收 actualQuery。
    field->setText(actualQuery);

    {
        // 析构时自动恢复进入前的信号阻断状态。
        const QSignalBlocker blocker(field);
        field->setText(visibleText);

        if (selectionStart >= 0) {
            const int start = qMin(selectionStart, visibleText.size());
            const int length = qMin(selectionLength,
                                    visibleText.size() - start);
            field->setSelection(start, qMax(0, length));
        } else {
            field->setCursorPosition(qMin(cursor, visibleText.size()));
        }
    }

    return true;
}
```

## 搜索框识别条件

只在以下条件全部满足时启用：

```text
搜索框类名       = Alg::SearchFieldLineEdit
搜索框 objectName = search_field
父级中存在       = Alg::NewResourcesView
资产视图类名     = Alg::ResourceListView
资产视图 objectName = resources
资产模型类名     = Alg::NewResourceListModel
```

Painter 升级后任何条件不匹配，都应返回失败，不进行宽泛控件扫描或猜测绑定。

## 关闭和回滚

插件关闭、功能关闭或者 Painter 重建 Assets 面板时：

1. 断开插件自己建立的信号连接。
2. 清除防递归状态和待处理任务。
3. 不删除、不替换 Painter 的模型或委托。
4. 如果内部查询与可见文本仍然分离，应主动让 Painter 按当前可见文本重新搜索，使原生状态恢复一致。

PySide6 可以在确认信号仍有效后执行：

```python
visible_text = search_field.text()
search_field.textChanged.emit(visible_text)
```

该同步操作必须在插件连接断开之后执行，避免再次进入插件逻辑。

## 限制和风险

- 本方案不会自行产生 `actual_query`，所以它本身不是中文搜索算法。
- `blockSignals(True)` 会暂时阻断该搜索框的所有 QObject 信号，不只是 `textChanged`；阻断范围必须限制在一次同步调用内，并用 `finally` 或 `QSignalBlocker` 保证恢复。
- 如果未来 Painter 改成延迟读取 `search_field.text()`，而不是保存 `textChanged` 参数，本方案会失效。因此只能对白名单版本启用，并需重新实测。
- 不应使用全局键鼠挂钩、宿主 EXE 补丁、Qt 导入表挂钩或模型替换来实现该功能。
- 不应长期保持信号阻断状态，也不能在异常路径中遗漏恢复。

