# 仓库整理说明

本文档说明仓库的目录结构、各部分职责，以及如何构建与打包。

## 目录结构

```text
sp插件/
├── source/                       # 全部源码
│   ├── sp_chinese_translation/   # 插件源码（唯一入口，打包时原样打成 zip）
│   │   ├── __init__.py           # 插件入口（全仓库仅此一份）
│   │   ├── packages/             # 运行时依赖（原生 DLL + py7zr / h5py / numpy 等）
│   │   │   └── sp_native_asset_delegate.dll
│   │   ├── translations/         # 中文词典（official_assets_zh.json）
│   │   └── THIRD_PARTY_LICENSES.txt # 第三方依赖许可（由目录合并为单个文件）
│   └── c++/                      # 原生 C++ 模块源码
│       ├── native_asset_delegate.cpp # DLL 源码
│       ├── CMakeLists.txt
│       ├── qt-sdk/               # 自带的 Qt 构建辅助文件（头文件 + CMake 配置）
│       └── build/                # CMake 构建产物（已加入 .gitignore，不入库）
├── scripts/
│   ├── diagnostics/              # 运行时诊断脚本（只读检查 Painter 控件结构）
│   ├── translation_work/         # 词典维护 / 合并 / 校对脚本
│   └── build_package.py          # 打包脚本：source/ → dist/sp_chinese_translation.zip
├── dist/
│   └── sp_chinese_translation.zip  # 发布包（解压即可安装到 Painter）
├── README.md                     # 安装与使用说明
└── 整理说明.md                    # 本文档
```

## 关于重复文件

原根目录 `sp_chinese_translation.py` 与 `package_compact/sp_chinese_translation/__init__.py` 是同一份代码
（SHA256 完全一致）。整理后合并为唯一入口：

- 源码统一放在 `source/sp_chinese_translation/__init__.py`；
- 发布时由 `scripts/build_package.py` 直接打包该目录，不再手工复制，避免两份代码日后漂移。

修改插件逻辑时只改这一份文件即可。

## 构建与打包

原生 DLL 需要 MSVC + CMake + Qt 6.5.3 (msvc2019_64)：

```text
cmake -S source/c++ -B source/c++/build -DQt6_DIR=.../source/c++/qt-sdk/6.5.3/msvc2019_64/lib/cmake/Qt6
cmake --build source/c++/build --config Release
```

打包（自动把新编译的 DLL 带进发布包；没有新 DLL 时沿用
`source/sp_chinese_translation/packages/` 里现有的）：

```text
python scripts/build_package.py
```

输出 `dist/sp_chinese_translation.zip`，zip 根目录即插件内容，可直接解压到
`...\python\plugins\sp_chinese_translation\` 安装。

> 注意：当前开发机未安装 MSVC / CMake，且自带 qt-sdk 缺少 Qt 导入库，
> 无法在本机重新编译 DLL。仓库当前随附的 DLL 来自 Git 历史提交中已验证的构建。

## 词典维护

`translation_work/` 下的脚本用于维护
`source/sp_chinese_translation/translations/official_assets_zh.json`：

- `merge_external_json.py` / `merge_1_calibrated.py`：合并外部词典
- `review_official_translations.py`：逐条校对修正
- `complete_official_translations.py` / `complete_extracted_translations.py`：批量补全

## 其他说明

- `source/c++/build/`（CMake 产物）与 `__pycache__/`（Python 缓存）已加入 `.gitignore`；
  `source/c++/build/` 已从 Git 跟踪中移除（磁盘文件保留，可随时重新生成）。
- 发布 zip 是生成物，任何时候都可以用打包脚本重新生成，不必手工维护。
